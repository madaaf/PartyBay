<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mada.partybay" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT"/>
    <uses-feature android:name="android.hardware.camera" />
    <used-feature android:name="android.hardware.camera.setParameters" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:name="android.hardware.screen.portrait" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Activity.Chargement"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

            <!-- PACKAGE ACTIVITY -->
        <activity
            android:name=".Activity.MyActivity"
            android:label="@style/AppTheme"/>
        <activity
            android:name=".Activity.Connexion"
            android:label="@style/AppTheme"/>
        <activity
            android:name=".Activity.FirstActivity"
            android:label="@style/AppTheme"/>
        <activity
            android:name=".Activity.Inscription"
            android:label="@style/AppTheme"/>
        <activity
            android:name=".Activity.popup"
            android:label="@style/AppTheme"/>

        <activity
            android:name=".Activity.Editer"
            android:label="@style/AppTheme"/>

        <activity
            android:name=".Activity.CameraActivity"
            android:label="@style/AppTheme"/>

        <activity
            android:name=".Activity.Activation"
            android:label="@style/AppTheme"/>


        <!-- PACKAGE MENU MANAGER -->

        <activity
             android:name=".MenuManager.ViewPagerActivity"
             android:label="@style/AppTheme"/>

        <activity
            android:name=".MenuManager.ViewPagerActivity"
            android:label="@style/AppTheme"/>

        <!-- PACKAGE TIMELINE -->
        <activity
            android:name=".TimeLineManager.PostActivity"
            android:label="@style/AppTheme"/>

        <!-- PACKAGE PROFILE MANAGER -->
        <activity
            android:name=".ProfileManager.ProfileViewPagerActivity"
            android:label="@style/AppTheme"/>

        <activity
            android:name=".ProfileManager.ItemAlbum"
            android:label="@style/AppTheme"/>








    </application>


</manifest>
